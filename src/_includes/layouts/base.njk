<!DOCTYPE html>
<html lang="sk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | AMSECURE</title>
    <meta name="description" content="{{ description or 'Profesionálne bezpečnostné riešenia, WiFi siete a smart domácnosti.' }}">
    <link rel="stylesheet" href="{{ '/css/styles.css' | url }}">
</head>
<body>
    <header>
        <div class="container">
            <a href="/" class="logo"><img src="{{ '/img/logo.png' | url }}" alt="AMSECURE Logo"></a>
            {% include "partials/nav.njk" %}
        </div>
    </header>

    <main>
        <div class="container">
            {{ content | safe }}
        </div>
    </main>

    <footer>
        <div class="container">
            <p>&copy; {% year %} AMSECURE. Všetky práva vyhradené.</p>
        </div>
    </footer>

    <script src="{{ '/js/main.js' | url }}" defer></script>
</body>
</html>
